// Retained downloads Live Activity development signing defaults.
// Override locally with Local.xcconfig; see Local.xcconfig.sample.
// The host supplies content through ActivityKit; this extension has no
// App Group or shared Keychain entitlement.

VIVID_DOWNLOADS_ACTIVITY_BUNDLE_ID = com.blurbery.vivid.DownloadsActivity

PRODUCT_BUNDLE_IDENTIFIER = $(VIVID_DOWNLOADS_ACTIVITY_BUNDLE_ID)

#include? "Local.xcconfig"
